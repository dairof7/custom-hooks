# useFetch

usage:

```
url = 'endpoint de una api'
const {data: null, loading: false, error: 'No se pudo cargar la info'} = useFetch(url)
const {data , loading: true, error: null} = useFetch(url)

```
